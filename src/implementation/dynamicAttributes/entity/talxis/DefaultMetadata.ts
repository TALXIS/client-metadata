export enum IntegerDefaultMetadata {
  MinValue = -2147483648,
  MaxValue = 2147483647
}

export enum TextDefaultMetadata {
  MaxLength = 100
}

export enum DecimalDefaultMetadata {
  MinValue = -100000000000,
  MaxValue = 100000000000,
  Precision = 2
}